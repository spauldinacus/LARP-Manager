#!/usr/bin/env node

// Development entry point that starts the Express server with Vite integration
// This replaces the standalone Vite dev server to provide full-stack functionality

import './server/index.ts';